requests>=2.31
flask>=3.0
yt-dlp>=2026.8.19

# macOS-only: native Dock badge, menu-bar item, notifications.
pyobjc-framework-Cocoa>=10.0; sys_platform == 'darwin'
py2app>=0.28; sys_platform == 'darwin'
dmgbuild>=1.6; sys_platform == 'darwin'

# Windows-only: notification-area (tray) icon and its glyph. Both are
# optional at runtime -- desktop_integration falls back to a no-op when they
# are missing -- but the frozen build bundles them so installed copies get
# the tray icon that keeps VDR reachable after its window is closed.
pystray>=0.19; sys_platform == 'win32'
pillow>=10.0; sys_platform == 'win32'

# Freezing is done on both desktop platforms (DMG on macOS, EXE + installer
# on Windows), so this is no longer macOS-gated.
pyinstaller>=6.0; sys_platform == 'darwin' or sys_platform == 'win32'
